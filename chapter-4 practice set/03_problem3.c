#include <stdio.h>

int main() {
    /*
      3. A do while loop is executed:
         a. At least once.
         b. At least twice.
         c. At most once.
    */
   
    // ✅ a. At least once. (कम से कम एक बार)
    return 0;
}